import { Container, Typography } from "@mui/material";

const Home = () => (
  <Container>
    <Typography variant="h4">Home Page</Typography>
    <Typography variant="body1">
      Welcome to the home page! Use the navigation to explore other sections.
    </Typography>
  </Container>
);
export default Home;
